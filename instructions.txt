git clone https://github.com/hughes6/budget.git 
cd budget 
python –m venv venv 
source venv/bin/activate 
python.exe -m pip install –upgrade pip 
sudo apt update
sudo apt install libpq-dev python3-dev -y
pip install psycopg2==2.9.11
pip install -r requirements.txt
vi .env 
DATABASE_HOSTNAME=localhost 
DATABASE_PORT=5432 
DATABASE_PASSWORD=Kgtwm7yssqp3n7! 
DATABASE_NAME=Budget 
DATABASE_USERNAME=postgres 
:wq
sudo apt install -y postgresql postgresql-contrib
sudo systemctl status postgresql


# check to see CPU temp
vcgencmd measure_temp

# Watch temperature change every 2 seconds
watch -n 2 vcgencmd measure_temp

# see if power throttled
vcgencmd get_throttled
